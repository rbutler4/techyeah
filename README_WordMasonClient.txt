This is a threaded client.
It has main but that might need to be changed to a constructor, destructor, and methods.
The parse method has the GUI method calls commented out in the form "GUI.method(args)".

Setting Boolean DEBUG in main to false turns off debuging info.

usage: client [host] [port]
default 127.0.0.1:5000

to run with defaults:
java client